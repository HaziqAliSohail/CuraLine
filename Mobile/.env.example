# Mobile (Expo) environment — copy to .env and fill in.
# EXPO_PUBLIC_-prefixed vars are inlined into the app bundle at build time.

# API base URL is set in src/config.js (dev LAN IP vs prod HTTPS).

# Error monitoring. Leave empty to disable Sentry entirely.
EXPO_PUBLIC_SENTRY_DSN=
