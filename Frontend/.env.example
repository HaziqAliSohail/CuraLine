# Frontend (Vite) environment — copy to .env and fill in.
# Only VITE_-prefixed vars are exposed to the browser bundle.

# Error monitoring. Leave empty to disable Sentry entirely (it won't even load).
VITE_SENTRY_DSN=
